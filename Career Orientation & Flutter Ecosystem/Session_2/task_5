5.Use ChatGPT or GitHub Copilot to suggest improvements to your MusicCard widget code (from task 2),
such as better widget structuring or styling, and implement at least one suggestion. Paste your code and ask for Flutter best practices or UI enhancements.

import 'package:flutter/material.dart';

void main() {
  runApp(MaterialApp(
    home: Scaffold(
      appBar: AppBar(
        title: Text("Music Card"),
      ),
      body: Center(
        child: Card(
          child: Padding(
            padding: EdgeInsets.all(20),
            child: Column(
              mainAxisSize: MainAxisSize.min,
              children: [
                Icon(Icons.music_note, size: 50),
                Text(
                  "Shape of You",
                  style: TextStyle(
                    fontSize: 20,
                    fontWeight: FontWeight.bold,
                  ),
                ),
                Text(
                  "Ed Sheeran",
                  style: TextStyle(fontSize: 16),
                ),
              ],
            ),
          ),
        ),
      ),
    ),
  ));
}
